import CircularSlider from "./CircularSlider";

const options = {
    container: "slider",
    color: "purple",
    max: 2000,
    min: 0,
    step: 10,
    radius: 200
};

const slider  = new CircularSlider(options);